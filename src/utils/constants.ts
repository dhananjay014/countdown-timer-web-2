export const MAX_TIMERS = 20;
export const MAX_EVENTS = 50;
export const TICK_INTERVAL = 250; // ms - update 4x/sec for smooth display
export const DEFAULT_SETTINGS: import('../types').Settings = {
  soundEnabled: true,
  volume: 70,
  theme: 'system',
  notificationsEnabled: false,
};
